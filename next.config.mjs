// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
    },
    pageExtensions: ['tsx', 'ts'],
  };
  
  export default nextConfig;
  